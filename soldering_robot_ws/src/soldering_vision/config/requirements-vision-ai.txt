# The Jetson-specific torch/torchvision wheels are installed separately.
# Do not let pip replace them with generic wheels.
numpy>=1.24.4,<2
opencv-python>=4.6,<4.12
ultralytics>=8.3,<9
# Install with SAM2_BUILD_CUDA=0 and --no-build-isolation on Jetson so pip
# reuses the CUDA-enabled torch wheel from requirements-jetson-ai.txt.
SAM-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f
wandb>=0.22.3,<1
# Ubuntu 22.04 supplies requests 2.25.x through ROS system packages.
urllib3>=1.26.11,<1.27
